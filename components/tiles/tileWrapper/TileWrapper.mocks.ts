// Mock props to test interface props
import { ITileWrapper } from './TileWrapper';

const base: ITileWrapper = {
  children: null,
};

const alt: ITileWrapper = {
  children: null,
};

export const mockTileWrapperProps = {
  base,
  alt,
};
