// Mock props to test interface props
import { IModal } from './Modal';

const base: IModal = {
  children: null
};

const alt: IModal = {
  children: null,
};

export const mockModalProps = {
  base,
  alt,
};
